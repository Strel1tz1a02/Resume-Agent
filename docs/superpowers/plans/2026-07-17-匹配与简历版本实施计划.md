# 匹配与简历版本实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 跑通 `JDAnalysis -> MatchReport -> ResumeVersion` 链路，并提供候选选材、独立 Markdown 编辑与预览页面。

**Architecture:** 后端用两个独立占位服务生成报告与 Markdown，路由负责用户权限、关系溯源和持久化；前端沿用页面级状态，将匹配报告嵌入岗位页，将简历版本放在独立页面。所有新行为按测试先行实现，候选材料与实际采用材料始终使用不同字段和 UI 表达。

**Tech Stack:** FastAPI、SQLAlchemy 2.x、Pydantic 2、pytest、React 18、TypeScript、Vitest、Testing Library、原生 CSS。

## Global Constraints

- 本阶段只跑通链路，不实现关键词匹配、相关性排序、证据判断、真实 LLM、Word/PDF 导出或投递。
- 简历生成服务只能消费 `MatchReport`、`Experience` 和 `Skill`，不得消费 `JobPosting` 或 `JDAnalysis`。
- ResumeVersion 的实际采用 ID 必须是 MatchReport 候选 ID 的无重复子集。
- 所有业务查询和写入必须限定 `DEFAULT_USER_ID`。
- 前端不新增路由、状态管理、富文本或 Markdown 第三方依赖。

---

### Task 1: MatchReport 占位服务与 API

**Files:**
- Create: `apps/api/app/schemas/matches.py`
- Create: `apps/api/app/services/match_placeholder.py`
- Create: `apps/api/app/routers/matches.py`
- Create: `apps/api/tests/test_matches_api.py`
- Modify: `apps/api/app/main.py`
- Modify: `apps/api/app/schemas/__init__.py`
- Modify: `apps/api/app/routers/__init__.py`

**Interfaces:**
- Consumes: `JDAnalysis`, 当前用户的 `Experience` 和 `Skill` ORM 记录。
- Produces: `create_match_placeholder(experiences, skills) -> MatchPlaceholderResult`；`POST /jd-analyses/{analysis_id}/match`、`GET /jd-analyses/{analysis_id}/match-reports`、`GET /match-reports/{report_id}`。

- [ ] **Step 1: 写失败的占位服务测试**

在 `apps/api/tests/test_matches_api.py` 创建画像、岗位和分析辅助函数，并先直接测试服务输出：

```python
def test_placeholder_match_uses_all_profile_materials() -> None:
    experiences = [Experience(id=2, user_id=DEFAULT_USER_ID, type="project", name="项目")]
    skills = [Skill(id=5, user_id=DEFAULT_USER_ID, description="FastAPI")]

    result = create_match_placeholder(experiences, skills)

    assert result.overall_score == 0.0
    assert result.candidate_experience_ids == [2]
    assert result.candidate_skill_ids == [5]
    assert result.resume_strategy == "占位匹配：生成简历前请确认实际采用的候选材料。"
```

- [ ] **Step 2: 运行测试确认因模块不存在而失败**

Run: `python -m pytest tests/test_matches_api.py::test_placeholder_match_uses_all_profile_materials -v`

Working directory: `apps/api`

Expected: FAIL，提示 `app.services.match_placeholder` 不存在。

- [ ] **Step 3: 实现最小占位服务**

在 `match_placeholder.py` 定义不可变结果类型和纯函数：

```python
@dataclass(frozen=True)
class MatchPlaceholderResult:
    overall_score: float
    candidate_experience_ids: list[int]
    candidate_skill_ids: list[int]
    matched_requirements: list[str]
    gaps: list[str]
    risks: list[str]
    follow_up_questions: list[str]
    resume_strategy: str


def create_match_placeholder(
    experiences: Sequence[Experience],
    skills: Sequence[Skill],
) -> MatchPlaceholderResult:
    has_materials = bool(experiences or skills)
    return MatchPlaceholderResult(
        overall_score=0.0,
        candidate_experience_ids=[item.id for item in experiences],
        candidate_skill_ids=[item.id for item in skills],
        matched_requirements=["占位匹配尚未评估具体要求"] if has_materials else [],
        gaps=[] if has_materials else ["画像库暂无可用经历或技能"],
        risks=["当前结果由占位服务生成，未进行相关性计算"],
        follow_up_questions=[] if has_materials else ["请先补充经历或技能后再确认选材。"],
        resume_strategy="占位匹配：生成简历前请确认实际采用的候选材料。",
    )
```

- [ ] **Step 4: 运行服务测试确认通过**

Run: `python -m pytest tests/test_matches_api.py::test_placeholder_match_uses_all_profile_materials -v`

Expected: PASS。

- [ ] **Step 5: 写 MatchReport API 失败测试**

补充测试验证创建、空画像、历史、详情和跨用户隔离：

```python
def test_match_report_creation_and_history(client: TestClient) -> None:
    analysis_id = _create_analysis(client)
    experience = client.post("/experiences", json={"type": "project", "name": "求职助手"}).json()
    skill = client.post("/skills", json={"category": "Backend", "description": "FastAPI"}).json()

    first = client.post(f"/jd-analyses/{analysis_id}/match")
    second = client.post(f"/jd-analyses/{analysis_id}/match")

    assert first.status_code == 201
    assert second.status_code == 201
    assert second.json()["candidate_experience_ids"] == [experience["id"]]
    assert second.json()["candidate_skill_ids"] == [skill["id"]]
    history = client.get(f"/jd-analyses/{analysis_id}/match-reports").json()
    assert [item["id"] for item in history] == [second.json()["id"], first.json()["id"]]
    assert client.get(f"/match-reports/{first.json()['id']}").status_code == 200
```

响应 schema 同时返回 `candidate_experiences: list[ExperienceRead]` 和 `candidate_skills: list[SkillRead]`，供前端直接展示。

- [ ] **Step 6: 运行 API 测试确认 404 失败**

Run: `python -m pytest tests/test_matches_api.py -v`

Expected: API 用例 FAIL，端点返回 404。

- [ ] **Step 7: 实现 schemas、路由和注册**

`MatchReportRead` 包含 ORM 字段和展开材料：

```python
class MatchReportRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    jd_analysis_id: int
    overall_score: float
    candidate_experience_ids: list[int]
    candidate_skill_ids: list[int]
    matched_requirements: list[str]
    gaps: list[str]
    risks: list[str]
    follow_up_questions: list[str]
    resume_strategy: str
    candidate_experiences: list[ExperienceRead]
    candidate_skills: list[SkillRead]
```

路由必须按 `DEFAULT_USER_ID` 查询分析、报告、Experience 和 Skill；创建时用 `asdict(create_match_placeholder(...))` 持久化。详情响应使用一个私有函数将候选 ID 展开为按 ID 顺序排列的材料列表。`main.py` 注册新 router。

- [ ] **Step 8: 运行 MatchReport 测试和完整后端测试**

Run: `python -m pytest tests/test_matches_api.py -v`

Expected: PASS。

Run: `python -m pytest -v`

Expected: 全部 PASS。

- [ ] **Step 9: 提交 MatchReport 链路**

```bash
git add apps/api/app apps/api/tests/test_matches_api.py
git commit -m "feat: add placeholder match report flow"
```

---

### Task 2: ResumeVersion 占位服务与 API

**Files:**
- Create: `apps/api/app/schemas/resumes.py`
- Create: `apps/api/app/services/resume_placeholder.py`
- Create: `apps/api/app/routers/resumes.py`
- Create: `apps/api/tests/test_resumes_api.py`
- Modify: `apps/api/app/main.py`
- Modify: `apps/api/app/schemas/__init__.py`
- Modify: `apps/api/app/routers/__init__.py`

**Interfaces:**
- Consumes: `create_resume_markdown(match_report, experiences, skills) -> ResumePlaceholderResult`，其中参数类型不包含 JobPosting/JDAnalysis。
- Produces: `POST /match-reports/{report_id}/resume-versions`、`GET /resume-versions`、`GET /resume-versions/{version_id}`、`PUT /resume-versions/{version_id}`。

- [ ] **Step 1: 写失败的简历占位服务测试**

```python
def test_resume_placeholder_only_uses_selected_facts() -> None:
    report = MatchReport(id=9, user_id=DEFAULT_USER_ID, jd_analysis_id=3)
    experience = Experience(id=2, user_id=DEFAULT_USER_ID, type="project", name="求职助手", result="跑通闭环")
    skill = Skill(id=5, user_id=DEFAULT_USER_ID, category="Backend", description="FastAPI")

    result = create_resume_markdown(report, [experience], [skill])

    assert "# 简历草稿" in result.markdown_content
    assert "求职助手" in result.markdown_content
    assert "跑通闭环" in result.markdown_content
    assert "FastAPI" in result.markdown_content
```

- [ ] **Step 2: 运行确认模块不存在**

Run: `python -m pytest tests/test_resumes_api.py::test_resume_placeholder_only_uses_selected_facts -v`

Expected: FAIL，提示服务模块不存在。

- [ ] **Step 3: 实现固定 Markdown 模板**

```python
@dataclass(frozen=True)
class ResumePlaceholderResult:
    markdown_content: str
    generation_rationale: str


def create_resume_markdown(
    match_report: MatchReport,
    experiences: Sequence[Experience],
    skills: Sequence[Skill],
) -> ResumePlaceholderResult:
    lines = ["# 简历草稿", "", "## 经历"]
    lines.extend(_experience_lines(experiences) or ["- 暂未选择经历"])
    lines.extend(["", "## 技能"])
    lines.extend(f"- {item.description}" for item in skills)
    if not skills:
        lines.append("- 暂未选择技能")
    return ResumePlaceholderResult(
        markdown_content="\n".join(lines),
        generation_rationale="占位生成：Markdown 仅按用户确认的画像事实拼接。",
    )
```

`_experience_lines` 只组合已有非空字段，不推断或改写事实。

- [ ] **Step 4: 运行服务测试确认通过**

Run: `python -m pytest tests/test_resumes_api.py::test_resume_placeholder_only_uses_selected_facts -v`

Expected: PASS。

- [ ] **Step 5: 写生成、校验、列表与编辑失败测试**

覆盖以下断言：

```python
created = client.post(
    f"/match-reports/{report_id}/resume-versions",
    json={"used_experience_ids": [experience_id], "used_skill_ids": []},
)
assert created.status_code == 201
assert created.json()["used_experience_ids"] == [experience_id]
assert created.json()["used_skill_ids"] == []
assert created.json()["job_posting_id"] == job_id

invalid = client.post(
    f"/match-reports/{report_id}/resume-versions",
    json={"used_experience_ids": [999], "used_skill_ids": []},
)
assert invalid.status_code == 422

duplicate = client.post(
    f"/match-reports/{report_id}/resume-versions",
    json={"used_experience_ids": [experience_id, experience_id], "used_skill_ids": []},
)
assert duplicate.status_code == 422
```

更新测试保存原 `used_*_ids`，并断言 `manual_edit_history` 新增一条含 `edited_at`、`before_summary`、`after_summary` 的记录。

- [ ] **Step 6: 运行 API 测试确认端点缺失**

Run: `python -m pytest tests/test_resumes_api.py -v`

Expected: API 用例 FAIL，端点返回 404。

- [ ] **Step 7: 实现 schema、路由和注册**

创建 payload 使用字段验证器拒绝重复 ID：

```python
class ResumeVersionCreate(BaseModel):
    used_experience_ids: list[int] = Field(default_factory=list)
    used_skill_ids: list[int] = Field(default_factory=list)

    @field_validator("used_experience_ids", "used_skill_ids")
    @classmethod
    def ids_must_be_unique(cls, value: list[int]) -> list[int]:
        if len(value) != len(set(value)):
            raise ValueError("IDs must be unique")
        return value
```

路由创建版本前完成：报告权限校验、子集校验、材料归属校验、`MatchReport -> JDAnalysis -> JobPosting` 溯源。调用服务时只传报告和材料。列表与详情响应展开 `used_experiences`、`used_skills`，并包含 `job_company`、`job_title` 作为展示字段。

更新 Markdown 时，在 commit 前追加：

```python
history = list(version.manual_edit_history)
history.append({
    "edited_at": datetime.now(UTC).isoformat(),
    "before_summary": version.markdown_content[:120],
    "after_summary": payload.markdown_content[:120],
})
version.manual_edit_history = history
version.markdown_content = payload.markdown_content
```

- [ ] **Step 8: 运行简历 API 和完整后端测试**

Run: `python -m pytest tests/test_resumes_api.py -v`

Expected: PASS。

Run: `python -m pytest -v`

Expected: 全部 PASS。

- [ ] **Step 9: 提交简历后端链路**

```bash
git add apps/api/app apps/api/tests/test_resumes_api.py
git commit -m "feat: add resume version API flow"
```

---

### Task 3: 前端 MatchReport 与 ResumeVersion API 客户端

**Files:**
- Create: `apps/web/src/api/matches.ts`
- Create: `apps/web/src/api/matches.test.ts`
- Create: `apps/web/src/api/resumes.ts`
- Create: `apps/web/src/api/resumes.test.ts`

**Interfaces:**
- Consumes: Task 1、2 的 JSON API。
- Produces: `createMatchReport`、`listMatchReports`、`getMatchReport`、`createResumeVersion`、`listResumeVersions`、`getResumeVersion`、`updateResumeVersion` 及共享 TypeScript 类型。

- [ ] **Step 1: 写 API 客户端失败测试**

```typescript
it("creates a match report for an analysis", async () => {
  const fetchMock = vi.spyOn(globalThis, "fetch").mockResolvedValue(jsonResponse(report, 201));
  await expect(createMatchReport(41)).resolves.toEqual(report);
  expect(fetchMock).toHaveBeenCalledWith(
    "http://127.0.0.1:8000/jd-analyses/41/match",
    expect.objectContaining({ method: "POST" }),
  );
});

it("creates a resume from the confirmed subset", async () => {
  const payload = { used_experience_ids: [7], used_skill_ids: [] };
  const fetchMock = vi.spyOn(globalThis, "fetch").mockResolvedValue(jsonResponse(version, 201));
  await createResumeVersion(12, payload);
  expect(JSON.parse(fetchMock.mock.calls[0][1]?.body as string)).toEqual(payload);
});
```

- [ ] **Step 2: 运行确认模块不存在**

Run: `pnpm test -- src/api/matches.test.ts src/api/resumes.test.ts`

Working directory: `apps/web`

Expected: FAIL，提示模块不存在。

- [ ] **Step 3: 实现类型与 fetch 封装**

复用当前客户端的 `API_BASE_URL`/错误风格，类型字段与 Pydantic 响应完全一致。每个函数检查 `response.ok`，失败时抛出 `Error`，并使用 `Content-Type: application/json` 发送 JSON body。

- [ ] **Step 4: 运行 API 客户端测试与前端全测**

Run: `pnpm test -- src/api/matches.test.ts src/api/resumes.test.ts`

Expected: PASS。

Run: `pnpm test`

Expected: 全部 PASS。

- [ ] **Step 5: 提交前端客户端**

```bash
git add apps/web/src/api
git commit -m "feat: add match and resume web clients"
```

---

### Task 4: 岗位页匹配报告和选材流程

**Files:**
- Modify: `apps/web/src/features/jobs/JobsPage.tsx`
- Modify: `apps/web/src/features/jobs/JobsPage.test.tsx`
- Modify: `apps/web/src/styles/global.css`

**Interfaces:**
- Consumes: `createMatchReport(analysisId)`、`listMatchReports(analysisId)`、`createResumeVersion(reportId, payload)`。
- Produces: `JobsPageProps.onResumeCreated(versionId: number): void`，供 App 切换到简历页。

- [ ] **Step 1: 写生成并展示报告的失败测试**

测试 mock 岗位、分析、空报告历史和新报告响应：

```typescript
render(<JobsPage onResumeCreated={onResumeCreated} />);
await screen.findByDisplayValue("当前 React 要求");
fireEvent.click(screen.getByRole("button", { name: "生成匹配报告" }));
expect(await screen.findByText("占位匹配：生成简历前请确认实际采用的候选材料。")).toBeInTheDocument();
expect(screen.getByText("校园招聘助手")).toBeInTheDocument();
expect(screen.getByText("FastAPI")).toBeInTheDocument();
```

同时断言重新生成后历史选择器包含旧报告和新报告，且新报告成为当前项。

- [ ] **Step 2: 运行确认 UI 缺失**

Run: `pnpm test -- src/features/jobs/JobsPage.test.tsx`

Expected: 新测试 FAIL，找不到“生成匹配报告”。

- [ ] **Step 3: 实现报告状态、请求隔离和展示区**

为每个分析维护历史加载 epoch；分析变化时清空旧报告并加载新历史。报告区提供创建按钮、历史选择器、分数字段、文本列表、候选经历/技能和错误重试。旧历史请求只在当前岗位与当前分析仍匹配时写入状态。

- [ ] **Step 4: 运行报告 UI 测试确认通过**

Run: `pnpm test -- src/features/jobs/JobsPage.test.tsx`

Expected: 报告展示用例 PASS，既有第四阶段用例仍通过。

- [ ] **Step 5: 写默认全选、调整和失败重试测试**

```typescript
fireEvent.click(screen.getByRole("button", { name: "生成简历" }));
const experienceCheckbox = screen.getByRole("checkbox", { name: /校园招聘助手/ });
const skillCheckbox = screen.getByRole("checkbox", { name: /FastAPI/ });
expect(experienceCheckbox).toBeChecked();
expect(skillCheckbox).toBeChecked();
fireEvent.click(skillCheckbox);
fireEvent.click(screen.getByRole("button", { name: "确认生成" }));
expect(JSON.parse(fetchMock.mock.calls.at(-1)?.[1]?.body as string)).toEqual({
  used_experience_ids: [7],
  used_skill_ids: [],
});
await waitFor(() => expect(onResumeCreated).toHaveBeenCalledWith(31));
```

失败用例先返回 500，断言弹窗保持打开、选择不变，再次点击可成功。

- [ ] **Step 6: 运行确认选材弹窗缺失**

Run: `pnpm test -- src/features/jobs/JobsPage.test.tsx`

Expected: 新选材测试 FAIL。

- [ ] **Step 7: 实现选材弹窗和创建回调**

打开弹窗时从当前报告复制候选 ID 到本地 Set；复选框只修改本地选择；提交调用 `createResumeVersion`。成功后关闭弹窗并调用 `onResumeCreated(id)`，失败时只显示错误，不重置选择。

- [ ] **Step 8: 运行岗位页测试和前端全测**

Run: `pnpm test -- src/features/jobs/JobsPage.test.tsx`

Expected: PASS。

Run: `pnpm test`

Expected: 全部 PASS。

- [ ] **Step 9: 提交岗位匹配 UI**

```bash
git add apps/web/src/features/jobs apps/web/src/styles/global.css
git commit -m "feat: add match and resume selection workflow"
```

---

### Task 5: 独立简历版本页面和 App 集成

**Files:**
- Create: `apps/web/src/features/resumes/ResumeVersionsPage.tsx`
- Create: `apps/web/src/features/resumes/ResumeVersionsPage.test.tsx`
- Create: `apps/web/src/components/MarkdownPreview.tsx`
- Create: `apps/web/src/components/MarkdownPreview.test.tsx`
- Modify: `apps/web/src/App.tsx`
- Modify: `apps/web/src/App.test.tsx`
- Modify: `apps/web/src/styles/global.css`

**Interfaces:**
- Consumes: Task 3 的简历 API；`initialVersionId: number | null`。
- Produces: 独立页面的列表、选择、编辑、基础安全预览、保存和实际材料展示。

- [ ] **Step 1: 写基础 Markdown 预览失败测试**

```typescript
render(<MarkdownPreview markdown={"# 简历草稿\n\n## 经历\n- 项目 A\n\n普通段落"} />);
expect(screen.getByRole("heading", { level: 1, name: "简历草稿" })).toBeInTheDocument();
expect(screen.getByRole("heading", { level: 2, name: "经历" })).toBeInTheDocument();
expect(screen.getByRole("listitem", { name: "项目 A" })).toBeInTheDocument();
expect(screen.getByText("普通段落")).toBeInTheDocument();
```

另加 `<script>alert(1)</script>` 测试，断言它只作为文本显示，不使用 `dangerouslySetInnerHTML`。

- [ ] **Step 2: 运行确认组件不存在**

Run: `pnpm test -- src/components/MarkdownPreview.test.tsx`

Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现基础安全预览**

按行解析 `# `、`## `、`- ` 和普通非空段落，创建 React 元素；连续列表项组合为 `<ul>`。不解析 HTML，不调用 `dangerouslySetInnerHTML`。

- [ ] **Step 4: 运行预览测试确认通过**

Run: `pnpm test -- src/components/MarkdownPreview.test.tsx`

Expected: PASS。

- [ ] **Step 5: 写简历页加载、选择、编辑和错误测试**

```typescript
render(<ResumeVersionsPage initialVersionId={31} />);
expect(await screen.findByRole("button", { name: /示例科技.*后端工程师/ })).toBeInTheDocument();
expect(screen.getByLabelText("Markdown 内容")).toHaveValue(version.markdown_content);
expect(screen.getByText("校园招聘助手")).toBeInTheDocument();
expect(screen.queryByText("未采用技能")).not.toBeInTheDocument();

fireEvent.change(screen.getByLabelText("Markdown 内容"), { target: { value: "# 修改后" } });
expect(screen.getByRole("heading", { level: 1, name: "修改后" })).toBeInTheDocument();
fireEvent.click(screen.getByRole("button", { name: "保存简历" }));
```

保存失败测试断言 textarea 仍为“# 修改后”且可再次点击保存。迟到详情请求测试断言切到版本 B 后，版本 A 的响应不会覆盖 B。

- [ ] **Step 6: 运行确认简历页不存在**

Run: `pnpm test -- src/features/resumes/ResumeVersionsPage.test.tsx`

Expected: FAIL，模块不存在。

- [ ] **Step 7: 实现简历版本页面**

加载列表后优先选择 `initialVersionId`，否则选择第一项；使用 selection epoch 隔离详情请求。编辑器草稿与已保存版本分离，即时传给 `MarkdownPreview`。实际材料列表只读取 `used_experiences` 和 `used_skills`。

- [ ] **Step 8: 运行简历页测试确认通过**

Run: `pnpm test -- src/features/resumes/ResumeVersionsPage.test.tsx`

Expected: PASS。

- [ ] **Step 9: 写 App 导航失败测试**

扩展 `App.test.tsx`：点击“简历版本”加载独立页面；模拟岗位页成功创建版本后，断言页面标题切换为“简历版本”，并选中新版本。为 `JobsPage` 传入：

```tsx
<JobsPage
  onResumeCreated={(versionId) => {
    setPendingResumeVersionId(versionId);
    setActivePage("resumes");
  }}
/>
```

- [ ] **Step 10: 运行确认 App 尚未集成**

Run: `pnpm test -- src/App.test.tsx`

Expected: 新导航用例 FAIL。

- [ ] **Step 11: 集成 App 和 Agent 面板并补充样式**

将现有 resumes 占位导航接入 `ResumeVersionsPage`；工作台眉题显示“阶段 5”；简历页 Agent 文案说明“这里只展示实际采用材料，当前 Markdown 由占位服务按事实拼接”。补充报告区、候选弹窗、两栏编辑预览与响应式样式。

- [ ] **Step 12: 运行前端完整测试和构建**

Run: `pnpm test`

Expected: 全部 PASS。

Run: `pnpm build`

Expected: exit 0，无 TypeScript 或 Vite 构建错误。

- [ ] **Step 13: 提交独立简历页**

```bash
git add apps/web/src
git commit -m "feat: add resume version editor and preview"
```

---

### Task 6: 第五阶段端到端验收与文档更新

**Files:**
- Modify: `README.md`
- Modify: `docs/superpowers/plans/2026-07-17-匹配与简历版本实施计划.md`

**Interfaces:**
- Consumes: 前五个任务的完整链路。
- Produces: 可复现的本地启动说明和已勾选的计划记录。

- [ ] **Step 1: 运行后端完整测试**

Run: `python -m pytest -v`

Working directory: `apps/api`

Expected: 全部 PASS，0 failures。

- [ ] **Step 2: 运行前端完整测试**

Run: `pnpm test`

Working directory: `apps/web`

Expected: 全部 PASS，0 failures。

- [ ] **Step 3: 运行前端生产构建**

Run: `pnpm build`

Working directory: `apps/web`

Expected: exit 0。

- [ ] **Step 4: 更新 README**

在现有功能说明中加入：生成占位匹配报告、从默认全选候选材料中确认选材、生成并编辑 Markdown 简历版本。明确匹配与生成算法仍是占位实现。

- [ ] **Step 5: 对照 spec 逐项验收**

逐项确认：报告可生成与查看；候选与实际 ID 分离；默认全选可调整；生成后进入独立简历页；Markdown 可编辑、预览和保存；服务边界测试存在；失败状态保留草稿。

- [ ] **Step 6: 更新本计划 checkbox 并检查差异**

Run: `git diff --check`

Expected: 无输出，exit 0。

- [ ] **Step 7: 提交验收文档**

```bash
git add README.md docs/superpowers/plans/2026-07-17-匹配与简历版本实施计划.md
git commit -m "docs: complete phase 5 match and resume workflow"
```
